<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>EconomyAdmiral</RootNamespace>
    <AssemblyName>Economy-Admiral</AssemblyName>
    <Version>0.1.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="SPTarkov.Server.Core" Version="4.1.2" />
  </ItemGroup>

  <Target Name="CopyEconomyAdmiralConfig" AfterTargets="Build" Condition="Exists('$(ProjectDir)..\config\config.json')">
    <MakeDir Directories="$(OutputPath)config" />
    <Copy SourceFiles="$(ProjectDir)..\config\config.json" DestinationFolder="$(OutputPath)config" SkipUnchangedFiles="true" />
  </Target>
</Project>
